# Clase 7: polimorfismo

Proyecto Java sin herramientas de construccion. Para abrirlo: en VSCode,
Archivo -> Abrir carpeta, y elegir esta carpeta (la que contiene src y lib).

Todas las clases del catalogo llegan intactas de la clase pasada y no se
tocan. El unico archivo que se modifica es Tienda.java.

- `src/Producto.java`, `src/Libro.java`, `src/Electronico.java`,
  `src/Alimento.java`, `src/Descontable.java` y `src/Suscripcion.java`:
  completos y funcionando. No se modifican.
- `src/Tienda.java`: el programa del local, con los TODO de las partes 2 y 3.
- `src/Indumentaria.java`: no existe todavia. Se escribe en la parte 3.

Para ejecutar, abrir Tienda.java y usar el boton Run que aparece sobre el
metodo main.

Las consignas estan en el enunciado de la actividad.
